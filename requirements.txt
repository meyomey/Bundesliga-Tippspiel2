# =====================================================
# Wulmstörper Tipprunde - Core Dependencies
# Funktioniert auf Windows, Linux und Netcup Shared Hosting
# =====================================================
# WICHTIG: Alle Pins muessen Python 3.9 unterstuetzen!
# Netcup-Plesk bietet aktuell nur 3.9 als waehlbare Version;
# die CI testet 3.9 deshalb als Zielversion (Matrix 3.9-3.13).

Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.2.1
Flask-Mail==0.10.0
WTForms==3.2.2
email-validator==2.2.0
Werkzeug==3.1.8
requests==2.32.5
python-dotenv==1.2.1
itsdangerous==2.2.0
Pillow==11.3.0
reportlab==4.2.2

# Optional - nur fuer Standalone-Server (NICHT fuer Plesk/Netcup):
gunicorn==23.0.0
APScheduler==3.10.4

# Optional - fuer Web Push Benachrichtigungen (haeufig nicht auf Shared Hosting verfuegbar):
# pywebpush==2.0.0

# Optional - fuer PostgreSQL/MySQL statt SQLite:
psycopg2-binary==2.9.12
# PyMySQL==1.1.1

# =====================================================
# Neue Features: Redis, Testing, KI-Tippgegner
# =====================================================
redis==5.0.8
flask-caching==2.3.0

# Testing
pytest==8.4.2
pytest-flask==1.3.0
pytest-cov==5.0.0
factory-boy==3.3.1
faker==28.4.1

# Pure Python algorithms are now used instead of heavy C-extensions (numpy, scikit-learn).
# This ensures maximum compatibility on shared hosting environments like Netcup, Plesk, etc.

# Rate Limiting (brute-force protection)
Flask-Limiter==3.5.1
bleach==6.2.0
